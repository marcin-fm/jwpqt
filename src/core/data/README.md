# Portable compatibility tables

These compile-time include files are consumed by the portable core. They retain
the exact JIS and Windows code-page mappings recovered from JWPxp 1.67 while
keeping obsolete Win32 implementation files out of the native source tree.

The original files and attribution are preserved by the `jwpxp-1.67` tag.
