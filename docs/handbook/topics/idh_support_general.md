# Support

JWPqt 2.02 is a native Qt 6 continuation of JWPxp 1.67, descended from JWPce and JWP. Its recovered user-visible command and configuration surface has been audited: a command has either a tested native implementation or a documented platform replacement or exclusion. The running version is available in About status information and through `jwpqt --version`. Help > Contents opens the embedded offline handbook; F1 routes owning editors and dialogs to relevant topics without loading arbitrary local files or websites.

For local diagnostics, use **Help > Runtime Resources** or `jwpqt --resource-report`. They show loaded sources, failed resources, and skipped invalid records. A zero report exit status does not certify that every optional resource is available. Do not publish private document text, history archives, user dictionaries, or paths without reviewing them first. The editor and handbook can run without optional lookup data, while a partial WNN pair or corrupt required configuration is an error.

The native application replaces several historical support assumptions rather than emulating them. CMake/CPack and native desktop/Web delivery replace Windows Install and registry setup. The embedded handbook replaces WinHelp. Portable Qt MIME formats replace private Windows clipboard formats. Host desktops own system input-method state. PDF rendering is the accepted printing verification gate; physical-printer hardware was not tested. JWPqt supports Linux, Windows, macOS, and WebAssembly, but WebAssembly uses browser upload/download and IndexedDB, with project workspaces and printing visibly unavailable because browser file ownership and Qt WebAssembly PrintSupport cannot provide them.

The program's native source is GPL-2.0-or-later and comes without warranty, including merchantability or fitness for a particular purpose. Qt is dynamically linked and separately licensed. Original program credits and notices remain relevant: JWPce program and documentation are credited to Glenn Rosenthal, JWP origin and interface to Stephen Chung, and JWPxp/native contributions are retained in source history. Read the full GPL and preserved original notices rather than relying on a shortened summary.

Lookup data, fonts, and related information are separate from GPL program code. WNN, EDICT, KANJIDIC-derived, SKIP, frequency, radical, and stroke data have their own notices and conditions. Normal packages include original lookup payloads as separate unmodified files; their inclusion does not relicense them. For package and configuration boundaries, see [Dictionary Support](help:IDH_SUPPORT_DICTIONARY), [Future Releases](help:IDH_SUPPORT_FUTURE), and [Installing Additional Fonts](help:IDH_FONTS_INSTALL).
